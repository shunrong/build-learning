{
  "name": "01-esbuild-demo",
  "version": "1.0.0",
  "description": "esbuild 极速构建演示",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "build:prod": "node build.js --minify"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}

