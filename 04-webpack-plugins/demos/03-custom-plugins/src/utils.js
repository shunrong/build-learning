export function greeting(name) {
  return `Hello, ${name}!`;
}

export function add(a, b) {
  console.log(`计算: ${a} + ${b}`);
  return a + b;
}

export function multiply(a, b) {
  console.log(`计算: ${a} * ${b}`);
  return a * b;
}

